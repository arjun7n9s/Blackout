<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!--
      There is deliberately NO android.permission.INTERNET.
      OCR and both LLMs run entirely on-device, so a cloud call in the redact path is not just
      absent by policy - it is impossible for this process to make one.

      ML Kit's datatransport-cct and LiteRT's media3 dependency merge INTERNET /
      ACCESS_NETWORK_STATE into the APK. Strip them so dumpsys matches the comment above.
    -->
    <uses-permission android:name="android.permission.INTERNET" tools:node="remove" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" tools:node="remove" />

    <!-- required=false so the app still installs on camera-less devices -->
    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:enableOnBackInvokedCallback="true"
        android:theme="@style/Theme.Blackout">

        <!-- Vendor / bundled QNN. required=false so install still works off Qualcomm.
             Declaring libOpenCL.so is what made GPU warm-up succeed on OriginOS. -->
        <uses-native-library android:name="libvndksupport.so" android:required="false" />
        <uses-native-library android:name="libOpenCL.so" android:required="false" />
        <uses-native-library android:name="libcdsprpc.so" android:required="false" />
        <uses-native-library android:name="libQnnHtp.so" android:required="false" />
        <uses-native-library android:name="libQnnSystem.so" android:required="false" />
        <uses-native-library android:name="libQnnHtpV81Stub.so" android:required="false" />
        <uses-native-library android:name="libQnnHtpV81Skel.so" android:required="false" />
        <uses-native-library android:name="libQnnHtpPrepare.so" android:required="false" />
        <uses-native-library android:name="libQnnIr.so" android:required="false" />
        <uses-native-library android:name="libQnnSaver.so" android:required="false" />

        <!-- singleTop: re-sharing an image into an already-open Blackout should reuse the
             activity, not stack a second copy holding a second capture. It is also what makes
             onNewIntent fire, which the debug fixture harness needs to run a page warm. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:launchMode="singleTop"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode"
            android:theme="@style/Theme.Blackout">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Redact an image shared in from another app (e.g. a screenshot). -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:mimeType="image/*" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
